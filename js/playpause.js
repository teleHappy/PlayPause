(function(){ // start IIFE

	var increment, step, rotate;
	var top, bottom, rotate;
	var $bgCircle;

	init = function(){
		increment = 0;
		step = 0;
		rotate = 0;
		// apply default styles
		
		$bgCircle = $('#bgCircle')

		console.log($bgCircle);

	}

	init()

})(); // end IIFE